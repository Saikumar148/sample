name: shell commands

on: [push]

jobs:
  run-shell-commands:
    runs-on: windows-latest
    steps:
      - name: string
        run: echo "welcome to Devopshint"
      - name: multiline scripts
        run: |
          node -v
          npm -v